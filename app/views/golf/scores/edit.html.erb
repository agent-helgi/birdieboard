<h1>Edit Scores — Day <%= @round.day_number %></h1>
<p class="subtitle"><%= @tournament.name %> · <%= @round.course_name %></p>

<%= render "form", tournament: @tournament, round: @round, score: @score %>
